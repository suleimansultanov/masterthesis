const { Document, Packer, Paragraph, TextRun, HeadingLevel, AlignmentType } = require("docx");
const fs = require("fs");

// helper: body paragraph (justified, spacing)
const P = (runs) => new Paragraph({
  alignment: AlignmentType.JUSTIFIED,
  spacing: { after: 160, line: 276 },
  children: Array.isArray(runs) ? runs : [new TextRun(runs)],
});
const H1 = (t) => new Paragraph({ heading: HeadingLevel.HEADING_1, children: [new TextRun(t)] });
const H2 = (t) => new Paragraph({ heading: HeadingLevel.HEADING_2, children: [new TextRun(t)] });
// reference entry (hanging indent)
const REF = (t) => new Paragraph({
  spacing: { after: 120, line: 264 },
  indent: { left: 720, hanging: 720 },
  children: [new TextRun(t)],
});

const children = [
  H1("2. Literature Review"),
  P("This chapter reviews the body of work on which the proposed Adaptive Hybrid Kernel SVM builds. It is organised around four strands. The first establishes support vector machines (SVMs) and kernel methods as a default approach for high-dimensional biomedical data and shows that performance depends on matching the kernel to the structure of the data. The second examines Multiple Kernel Learning (MKL), the standard route to combining several kernels, and identifies the global-weighting assumption that this thesis sets out to relax. The third surveys noise handling and feature weighting in IoT healthcare, where sensor data are noisy, imbalanced and frequently mislabelled. The fourth gathers recent work on adaptive and hybrid kernel SVMs and dynamic feature weighting, which supplies the architectural building blocks for the proposed model. The chapter closes with a synthesis that locates the research gap at the intersection of these strands."),

  H2("2.1 SVMs and Kernel Methods for High-Dimensional Biomedical Data"),
  P("Support vector machines remain a strong default for biomedical classification, particularly in the high-dimensional, small-sample regime typical of gene-expression and microarray studies. The foundational demonstration of this is the SVM recursive feature elimination method of Guyon et al. (2002), which ranks genes by the magnitude of the SVM weight vector and recursively discards the least informative features. Beyond its empirical success, that work carries a conceptual point that runs through the whole of this thesis: the trained SVM weights themselves encode feature importance, so the model already contains a notion of which features matter. The proposed method extends this idea by learning feature weights explicitly and dynamically rather than reading them off after training."),
  P("A recurring theme in more recent work is that the choice of kernel should reflect the structure of the data rather than defaulting to a generic radial basis or polynomial function. Lubura et al. (2024) introduce a distance-based kernel tailored to binary and categorical features and show that a bespoke kernel can outperform off-the-shelf choices on both binary and multi-class problems. The recent review of kernel machines by Ghosh, Dasgupta et al. (2024) reaches a compatible conclusion at a broader level, surveying established and emerging kernel functions and positioning custom and composite kernel design as a central lever for improving SVM behaviour in difficult feature spaces. Together these works motivate the move away from a single fixed kernel toward hybrid, adaptively weighted constructions."),
  P("The biomedical setting that frames this thesis is defined precisely by high dimensionality, small samples and an abundance of redundant or noisy features. Akkur et al. (2024) address this directly with a hybrid filter and differential-evolution feature-selection pipeline that prunes thousands of irrelevant and noisy genes before classification, while Devi et al. (2025) apply a bio-inspired optimiser to select informative genes for downstream SVM classification. Mohammed et al. (2025) extend the picture to the fusion of multiple feature representations, reporting gains from combining complementary views of microarray data. These studies share an implicit assumption that different features and feature views contribute unequally to the decision, and that performance improves when this is accounted for. The present work internalises that assumption as learned per-feature weights rather than treating selection as a separate preprocessing stage."),

  H2("2.2 Multiple Kernel Learning (MKL)"),
  P("When no single kernel or feature view is adequate, Multiple Kernel Learning provides a principled route to combining several base kernels and learning their relative contributions. The standard reference is the survey of Gonen and Alpaydin (2011), which formalises MKL as the learning of a (typically convex or linear) combination of base kernels and categorises methods by their combination rule, optimisation strategy and weighting scheme. Critically for this thesis, that survey also makes explicit the dominant assumption of the field: most formulations assign a single global weight to each kernel, shared across every sample. This global-weighting assumption is the precise limitation the present work sets out to relax."),
  P("The principal exception within classical MKL is the localized variant introduced by Gonen and Alpaydin (2008), which uses a gating model to make kernel weights input-dependent, so that different regions of the input space draw on different kernel combinations. This is the closest classical precedent to the per-sample adaptivity proposed here. Subsequent work has improved its practicality and grounding: Han et al. (2013) reformulate localized MKL with a sample-wise alternating optimisation that learns per-sample kernel weights more efficiently and stably, demonstrating that per-sample weighting is feasible at scale, and Wang et al. (2022) derive sample-specific weights by maximising graph modularity, letting locality structure in the data drive each base kernel's local influence. These works show both that per-sample kernel weighting is achievable and that it can be grounded in data geometry rather than fixed coefficients."),
  P("Recent applications confirm the value of MKL in healthcare-adjacent settings while also illustrating the global-weighting limitation. The weight-boosted multi-kernel method of Cao et al. (2024) models each omics data type with several candidate kernels and learns kernel weights through a flexible weighting function for cancer subtype identification, yet the weighting operates at the data-type level and remains global across samples. Briscik et al. (2024) benchmark several supervised MKL strategies, including deep variants that fuse kernel-PCA embeddings through sub-networks, for integrating heterogeneous omics layers, providing a useful comparison space and showing the field drifting toward more flexible but still largely global weighting. Ghanizadeh et al. (2024) push this further by recasting MKL as a neural architecture, generalising the linear kernel combination so that weights can be learned end-to-end by gradient methods. This neural reframing is directly relevant to how the proposed model can parameterise adaptive, data-dependent kernel weights instead of solving a fixed convex combination."),

  H2("2.3 Noise Handling and Feature Weighting in IoT Healthcare"),
  P("Data collected from IoT healthcare devices are characteristically noisy, high in volume, frequently imbalanced and prone to unreliable labels, which places noise robustness at the centre of any practical model. The seminal contribution here is the fuzzy SVM of Lin and Wang (2002), which assigns each training point a fuzzy membership so that less reliable points, such as outliers and noisy samples, contribute less to the decision boundary. This established sample-level weighting as a general mechanism for noise robustness and is the conceptual basis for almost all subsequent noise-aware weighting, including the per-sample weights used in this thesis. Building in the same direction, Liu et al. (2024) add a tightness-based membership degree to support vector data description in order to identify and discount noisy-labelled points, a capability that maps directly onto the unreliable labels common in IoT-collected data."),
  P("At the system level, several recent studies characterise the IoT healthcare environment and the preprocessing it demands. Khan and Alkhathami (2024) model machine-learning techniques on IoT healthcare network traffic for anomaly detection, with preprocessing and feature handling designed to cope with noisy, high-volume streams. Saif et al. (2024) construct big-data and IoT health-informatics prediction pipelines that combine noise removal with feature analysis and selection before modelling, exemplifying the standard denoise-then-select workflow. Nayak et al. (2025) couple grey-filter denoising with a Bayesian convolutional network and metaheuristic optimisation for secure IoT healthcare classification under noisy inputs. A common feature of these pipelines is that noise handling and feature weighting are treated as separate stages that precede the classifier. The model proposed in this thesis instead aims to absorb noise-aware feature weighting into the kernel and weighting mechanism itself, unifying what these works keep apart. Robustness must also be reconciled with the resource limits of edge deployment, a constraint underscored by Anand et al. (2025/2026), who develop a robust and computationally light SVM for imbalanced and noisy data using Benders decomposition."),

  H2("2.4 Adaptive and Hybrid Kernel SVMs and Dynamic Feature Weighting"),
  P("The final strand assembles the architectural components of the proposed model. The theoretical licence for combining kernels comes from Scholkopf and Smola (2002), whose treatment of kernel methods establishes that sums and convex combinations of valid kernels remain valid kernels. This closure property is what legitimises hybrid kernels and adaptively weighted kernel combinations, and it is the mathematical foundation on which the thesis rests. The closest architectural template is the Bayesian-optimised hybrid kernel SVM of Zhang et al. (2023), which combines polynomial and radial basis kernels and tunes the penalty factor, kernel parameters and the mixing coefficient between the two kernels. Tellingly, that mixing weight is a single tuned constant, which is exactly the static-weight limitation the present work replaces with per-sample and per-feature adaptivity."),
  P("Several recent studies demonstrate that dynamic, data-driven adaptation of kernel parameters and feature weights is achievable in practice. An enhanced SVM model published in PLOS ONE (2025) introduces variance-based dynamic adjustment of kernel parameters, risk-aware kernel-weight optimisation and skewness-driven kernel-function switching, providing a concrete example of kernel behaviour driven by data statistics, albeit outside healthcare. A hybrid edge-based intrusion-detection method using an adaptive lightweight SVM with dynamic feature pruning (IEEE, 2025) couples adaptivity, dynamic feature weighting and IoT-edge constraints, the same three pressures this thesis faces, and so supports the feasibility of dynamic feature weighting in IoT settings. Singh et al. (2023) jointly optimise selected features and SVM hyperparameters with a particle-swarm method using dynamically adjusted inertia weight, demonstrating joint dynamic optimisation of feature weighting and model parameters, though at the dataset level rather than per sample."),
  P("Finally, hybrid adaptive architectures show how noise-aware weighting and kernel-based generalisation can be fused. The dynamically adaptive fuzzy-ANN-SVM architecture for agro-climatic forecasting (2025) integrates fuzzy logic, a neural network and an SVM into a single model whose components adjust to changing input conditions, offering a structural analogue to the fuzzy-weighted adaptive hybrid kernel SVM proposed here. In the biomedical domain, Tian et al. (2024) combine a custom kernel-based discriminative dictionary-learning module with convolutional features for explainable Alzheimer's and mild-cognitive-impairment classification, demonstrating custom learned kernels layered on deep features and reinforcing the emphasis on per-modality and per-feature contribution. Across these works the individual ingredients of the proposed model exist, but they appear in isolation rather than combined."),

  H2("2.5 Synthesis and Research Gap"),
  P("Two facts are settled across the first two strands. SVMs and kernel methods remain a strong default for high-dimensional, small-sample biomedical data, and their performance depends on matching the kernel to the structure of the data (Guyon et al., 2002; Lubura et al., 2024; Ghosh, Dasgupta et al., 2024). Where no single kernel suffices, MKL combines several kernels and learns their weights (Gonen and Alpaydin, 2011). The recurring limitation is that classical and most current MKL formulations, including recent biomedical systems such as wMKL (Cao et al., 2024) and supervised-MKL benchmarks (Briscik et al., 2024), learn a single global weighting shared by every sample. The localized-MKL line (Gonen and Alpaydin, 2008; Han et al., 2013; Wang et al., 2022) is the principal exception in making kernel weights input-dependent, but it was developed largely outside noisy IoT healthcare and does not couple localized kernel weighting to explicit, noise-aware feature weighting."),
  P("The remaining two strands supply the missing ingredients, but in separate lines of work. The noise-handling literature shows that per-sample membership weighting (Lin and Wang, 2002; Liu et al., 2024) and IoT-specific denoising and feature-selection pipelines (Khan and Alkhathami, 2024; Saif et al., 2024; Nayak et al., 2025) reliably improve robustness when sensor data are noisy, imbalanced or mislabelled. The adaptive and hybrid-kernel literature shows that hybrid kernels (Zhang et al., 2023), data-driven dynamic kernel parameters and weights (PLOS ONE, 2025; IEEE edge-IDS, 2025) and joint feature and parameter optimisation (Singh et al., 2023) are each individually feasible, and that kernel theory permits these compositions (Scholkopf and Smola, 2002). Yet hybrid-kernel SVMs still typically fix the kernel mixing as a single tuned constant, and adaptive feature-weighting methods rarely operate jointly with adaptive kernel combination."),
  P("The gap this thesis addresses sits precisely at the intersection these strands leave open: an SVM that combines multiple kernels with weights that adapt per sample rather than globally; that simultaneously learns per-feature weights to down-weight noisy or irrelevant sensor channels; and that does so under the noise, imbalance and resource constraints of IoT healthcare streams. Localized MKL contributes per-sample kernel adaptivity, fuzzy and weighted SVMs contribute noise-robust sample and feature weighting, and hybrid-kernel SVMs contribute the composite-kernel architecture, but no surveyed work unifies all three. An adaptive hybrid kernel SVM with dynamic feature weighting is therefore a well-motivated and, on the evidence above, technically achievable contribution."),

  H1("References"),
  REF("Akkur, E., et al. (2024). Enhancing Cancerous Gene Selection and Classification for High-Dimensional Microarray Data Using a Novel Hybrid Filter and Differential Evolutionary Feature Selection. Cancers, 16(23), 3913. MDPI."),
  REF("Anand, P., et al. (2025/2026). A robust and lightweight support vector machine for imbalanced and noisy data via Benders decomposition. Neurocomputing. Elsevier."),
  REF("Briscik, M., et al. (2024). Supervised Multiple Kernel Learning approaches for multi-omics data integration. BioData Mining, 17. Springer."),
  REF("Cao, H., Jia, C., Li, Z., et al. (2024). wMKL: multi-omics data integration enables novel cancer subtype identification via weight-boosted multi-kernel learning. British Journal of Cancer, 130, 1001-1012."),
  REF("Devi, R. M., et al. (2025). Cancer classification in high dimensional microarray gene expressions by feature selection using eagle prey optimization. Frontiers in Genetics, 16, 1528810."),
  REF("Ghanizadeh, A. N., Ghiasi-Shirazi, K., Monsefi, R., et al. (2024). Neural Generalization of Multiple Kernel Learning. Neural Processing Letters, 56, 12. Springer."),
  REF("Ghosh, S., Dasgupta, A., et al. (2024). Exploring Kernel Machines and Support Vector Machines: Principles, Techniques, and Future Directions. Mathematics, 12(24), 3935. MDPI."),
  REF("Gonen, M., & Alpaydin, E. (2008). Localized Multiple Kernel Learning. In Proceedings of the 25th International Conference on Machine Learning (ICML), 352-359."),
  REF("Gonen, M., & Alpaydin, E. (2011). Multiple Kernel Learning Algorithms. Journal of Machine Learning Research, 12, 2211-2268."),
  REF("Guyon, I., Weston, J., Barnhill, S., & Vapnik, V. (2002). Gene Selection for Cancer Classification using Support Vector Machines. Machine Learning, 46(1-3), 389-422."),
  REF("Han, Y., et al. (2013). Localized Multiple Kernel Learning Via Sample-Wise Alternating Optimization. IEEE Transactions on Cybernetics, 43(1), 137-148."),
  REF("Khan, M. M., & Alkhathami, M. (2024). Anomaly detection in IoT-based healthcare: machine learning for enhanced security. Scientific Reports, 14, 5872."),
  REF("Lin, C.-F., & Wang, S.-D. (2002). Fuzzy Support Vector Machines. IEEE Transactions on Neural Networks, 13(2), 464-471."),
  REF("Liu, Z., et al. (2024). The fuzzy support vector data description based on tightness for noisy label detection. Complex & Intelligent Systems, 10. Springer."),
  REF("Lubura, J., et al. (2024). A distance-based kernel for classification via Support Vector Machines. Frontiers in Artificial Intelligence, 7, 1287875."),
  REF("Mohammed, M., et al. (2025). Multimodal feature-optimized approaches for cancer classification using microarray gene expression analysis. Scientific Reports, 15."),
  REF("Nayak, S. R., et al. (2025). Enhancing IoT-based healthcare security with grey filter Bayesian CNN and optimization algorithms. Scientific Reports, 15."),
  REF("Saif, S., et al. (2024). Implementation of machine learning techniques with big data and IoT to create effective prediction models for health informatics. Biomedical Signal Processing and Control, 91. Elsevier."),
  REF("Scholkopf, B., & Smola, A. J. (2002). Learning with Kernels: Support Vector Machines, Regularization, Optimization, and Beyond. MIT Press, Cambridge, MA."),
  REF("Singh, A. K., et al. (2023). A Hybrid Particle Swarm Optimization Algorithm with Dynamic Adjustment of Inertia Weight Based on a New Feature Selection Method to Optimize SVM Parameters. Entropy."),
  REF("Tian, Y., et al. (2024). Multimodal classification of Alzheimer's disease and mild cognitive impairment using a custom MKSCDDL kernel over CNN with transparent decision-making."),
  REF("Wang, T., et al. (2022). Localized multiple kernel learning using graph modularity. Pattern Recognition Letters, 156. Elsevier."),
  REF("Zhang, S., et al. (2023). Bayesian-Optimized Hybrid Kernel SVM for Rolling Bearing Fault Diagnosis. Sensors, 23(11), 5137. MDPI."),
];

const doc = new Document({
  styles: {
    default: { document: { run: { font: "Times New Roman", size: 24 } } },
    paragraphStyles: [
      { id: "Heading1", name: "Heading 1", basedOn: "Normal", next: "Normal", quickFormat: true,
        run: { size: 32, bold: true, font: "Times New Roman" },
        paragraph: { spacing: { before: 300, after: 200 }, outlineLevel: 0 } },
      { id: "Heading2", name: "Heading 2", basedOn: "Normal", next: "Normal", quickFormat: true,
        run: { size: 28, bold: true, font: "Times New Roman" },
        paragraph: { spacing: { before: 220, after: 140 }, outlineLevel: 1 } },
    ],
  },
  sections: [{
    properties: { page: { size: { width: 12240, height: 15840 }, margin: { top: 1440, right: 1440, bottom: 1440, left: 1440 } } },
    children,
  }],
});

Packer.toBuffer(doc).then((buf) => {
  fs.writeFileSync("Chapter2_Literature_Review.docx", buf);
  console.log("written");
});
